# Example .env (프로젝트 루트에서 사용)
# 이 파일을 복사하여 실제 값으로 채운 후 .env 로 사용하세요.
# 민감한 값은 절대 레포에 커밋하지 마세요.

# Gemini AI API Configuration
GEMINI_API_KEY=your_gemini_api_key_here
GEMINI_API_URL=https://generativelanguage.googleapis.com/v1beta/models/gemini-1.5-flash:generateContent

# Git Configuration
DEFAULT_BASE_BRANCH=main
GITHUB_USERNAME=your_github_username
# Optional: GITHUB_TOKEN=your_github_token_if_needed

# Branch Naming Configuration
FEATURE_BRANCH_PREFIX=story/
HOTFIX_BRANCH_PREFIX=fix/

# Jira Configuration
JIRA_BASE_URL=https://your-company.atlassian.net/browse/

# Jenkins / CI / Local dev environment variables (예시 값)
FLUTTER_HOME=/opt/flutter
FLUTTER_ROOT=/opt/flutter

ANDROID_HOME=/opt/android-sdk
ANDROID_SDK_ROOT=/opt/android-sdk

JAVA_HOME=/usr/lib/jvm/java-17-openjdk-amd64

# macOS 전용 (필요 시 설정)
DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer

# PATH (예시) — CI/로컬 머신 환경에 맞게 설정하세요.
PATH=$PATH:$FLUTTER_HOME/bin:$ANDROID_HOME/cmdline-tools/bin:$ANDROID_HOME/platform-tools:$JAVA_HOME/bin

# Jenkins 관련 (실제 경로는 CI 환경에 맞게 secrets/credentials로 관리 권장)
JENKINS_HOME=/var/lib/jenkins
GOOGLE_APPLICATION_CREDENTIALS=/var/lib/jenkins/firebase-service-account.json

# ---------------------------------------------------------------------
# Jenkins 설정 예시 (스크립트에서 로드됩니다)
# IMPORTANT: 아래 민감값(JENKINS_PASSWORD 등)은 실제 레포에 커밋하지 마세요.
# CI 환경에서는 secrets / credential manager를 사용하세요.
#
# Jenkins 서버 URL (예: http://jenkins.company.com:8080)
JENKINS_URL=http://localhost:10060

# Jenkins 사용자명
JENKINS_USER=jenkins_user

# Jenkins 비밀번호 또는 API 토큰 (절대 레포에 커밋 금지 — 예시값만)
# JENKINS_PASSWORD=your_api_token_here

# 기본 리포지토 URL (스크립트 실행 시 -r 옵션으로 덮어쓸 수 있음)
REPO_URL=https://github.com/YOUR_ORGANIZATION/YOUR_PROJECT.git

# Jenkins CLI JAR 이름 (필요시 변경)
JENKINS_CLI_JAR=jenkins-cli.jar

# Optional default project prefix. If empty, the script will infer from REPO_URL.
# PROJECT_PREFIX=
# ---------------------------------------------------------------------
