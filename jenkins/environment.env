# Jenkins 환경 변수 설정 파일
# 이 파일을 Jenkins 노드의 /etc/environment에 추가하거나
# Jenkins 관리 > 시스템 설정 > Global properties에서 설정하세요

# Flutter 설정
FLUTTER_HOME=/opt/flutter
FLUTTER_ROOT=/opt/flutter

# Android 설정
ANDROID_HOME=/opt/android-sdk
ANDROID_SDK_ROOT=/opt/android-sdk

# Java 설정
JAVA_HOME=/usr/lib/jvm/java-17-openjdk-amd64

# iOS 설정 (macOS 노드에서만)
DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer

# PATH 설정
PATH=$PATH:$FLUTTER_HOME/bin:$ANDROID_HOME/cmdline-tools/bin:$ANDROID_HOME/platform-tools:$JAVA_HOME/bin

# Jenkins 설정
JENKINS_HOME=/var/lib/jenkins

# Firebase CLI
GOOGLE_APPLICATION_CREDENTIALS=/var/lib/jenkins/firebase-service-account.json
